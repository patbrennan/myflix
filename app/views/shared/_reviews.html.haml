%h3 Reviews ( #{reviews.size} )

%ul
  - reviews.each do |review|
    %article.review
      %li.row
        .col-sm-2
          %span
            %strong
              Rating: #{review.rating}/5
          %p
            by: #{link_to review.user.full_name, home_path}
        .col-sm-8
          %p
            #{review.description}
